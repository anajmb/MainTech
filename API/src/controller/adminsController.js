const { PrismaClient} = require("@prisma/client");
const prisma = new PrismaClient();


const adminsController = {

    // Create a new admin 
    // create: async (req, res) => {

    //     try {
    //         const { name, cpf, email,}
    //     }
    // }
}